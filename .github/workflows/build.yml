name: Build Android APK

on:
  push:
    branches: [ "main", "master" ]

jobs:
  build:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: ./DJTube_Android_Studio_Project

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '17'

    - name: Install Modern Gradle
      run: |
        sudo apt-get update
        sudo apt-get install -y gradle

    - name: Build Debug APK Directly
      run: gradle assembleDebug

    - name: Upload APK Artifact
      uses: actions/upload-artifact@v4
      with:
        name: app-debug
        path: DJTube_Android_Studio_Project/app/build/outputs/apk/debug/app-debug.apk
